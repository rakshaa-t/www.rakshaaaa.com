```markdown
# www.rakshaaaa.com

This repository contains the Next.js portfolio site for Raksha.

Basic steps to run locally:
1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`
3. Build for production: `npm run build`

This repo is set up to deploy to Vercel.
```
